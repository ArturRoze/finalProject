Comments:

0. Comments 1.-6., 11.-13., 15. are valid for whole project.

1. Please remove unused imports and unused methods from all classes.
Please, use clean code practices.
Please, write java doc for complicated classes, complicated fields, complicated methods.
All class field should be private.

2. In Main.main I see:
MainMenu mainMenu = new MainMenu();            
mainMenu.startGame();

If object is used at once, you do not need create separated variable for this object. So we can write:
new MainMenu().startGame();

3. In Main.main I see:
catch (Exception e) {            
e.printStackTrace();        
}

e.printStackTrace(); in catch is not informative, it would be better to write failure explanation, for example:
System.out.println("Error with starting main menu");

4. In Main.main I see:
catch (Exception e) {
It would be better to write more specific exception class

5. Always write at the left side interface/abstract class, for example:
List<Person> persons = new ArrayList<>();

6. All void comments should be removed, for example:
/** * */

7. boolean removeBookRoom(Room room);
I think better name method is:
cancelBookRoom

8. Why interface HotelService is empty?

9.  boolean unBookUser(Room aRoom);
I think better name method is:
cancelBookRoomByUser
or unbookRoomByUser

10. boolean bookUser(Room aRoom, User user);
I think better name method is:
bookRoomByUser

11. In UserServiceImpl I see:
public class UserServiceImpl implements UserService<User> {
    Crud<User> userDaoImpl = new UserDaoImpl();
    
    It should be:
   public class UserServiceImpl implements UserService<User> {    
      private Crud<User> userDaoImpl;
   
      public UserServiceImpl (Crud<User> userDaoImpl) {
         this.userDaoImpl = userDaoImpl;
      }
      
   }
   
   The same about all classes with dependencies
   
12. You can use java.nio library insted of java.io library

13. In FileManagerImpl I see:
@Override   
public DataStorage readData(String str) {       
    DataStorage testFile = Utils.readFile(str);      
    return testFile; 
}

you can write just:
@Override   
public DataStorage readData(String str) {                     
    return Utils.readFile(str); 
}

14. Crud need be not in model package, but in dao package

15. All shoul be in the same way
public Hotel(String name, String city) {       
id = IdGenerator.getRandomId();       
this.name = name;        
this.city = city;        
rooms = new HashSet<>();   
}

So please write this. for all fields in constructor

16.
    

